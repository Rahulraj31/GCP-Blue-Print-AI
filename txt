i want root agent to be in root folder and not in cloud root folder  and all be instructions should be at one place in instructions.py at root level or if you want you hcan have them for subagents as well seperatly but

the folder structure i tohough was 
gcal_agent / 
           subagents/ 
                                
         _init_.py
         agent.py 
         instructions.py 

and within subagents then we can two folder sof mapping and billing agent 


also cloud billing api has filter option in v2beta version can you use that so that returned data is limiated to what we want and not just returning everything related to particular service. like if i search for e2 medium vm in mumbai i should just get things related to that and not the entire compute engine skus
